<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.AppHisLib">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppHisLib">
        <activity android:name=".BaseActivity"></activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ComValLibroActivity" />
        <activity android:name=".VerLibroActivity" />
        <activity android:name=".RegistroActivity" />
        <activity android:name=".EditarPerfilActivity" />
        <activity android:name=".PerfilActivity" />
        <activity android:name=".LibrosActivity" />
        <activity android:name=".ContentMainActivity" />
    </application>

</manifest>